@model IEnumerable<TODOList.Models.SubTask>

@{
    ViewBag.Title = "SubTask";
}

<h2>SubTask</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Text)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Completion)
        </th>
        <th>

            @Html.DisplayNameFor(model => model.Task.Text)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Category.Text)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Text)
        </td>
          <td>
            @*@Html.DisplayFor(modelItem => item.Completion)*@
            
            @using (@Html.BeginForm("ChangeComplite", "SubTask",  item ))
            {          
            @Html.DisplayFor(modelItem => item.Completion)
            <input type="submit" value="Complite" />
               
            }
            
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Task.Text)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Category.Text)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubTaskId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SubTaskId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubTaskId })
        </td>
    </tr>
}

</table>
